# raw_spec.md
## 1) 목적: 주제
**계약서/법무 문서 검토 보조 웹앱**을 만든다. 사용자가 계약서(예: NDA, 용역/매매/라이선스 계약 등)를 업로드하면:

1. 계약서에서 **위험 조항 / 조건 / 예외 사항**을 자동으로 찾아 **정해진 스키마(JSON)** 로 구조화 추출한다.  
   - 예: 손해배상/면책, 책임 한도, 계약 해지, 준거법/관할, 비밀유지, 개인정보, 지체상금, 대금/정산, 자동갱신, 지재권, 하도급 제한 등
2. 사내 “**표준 조항 라이브러리**(템플릿/권고 문구/가이드)”를 **VectorDB**로 구축하고, 추출된 위험 조항과 비교하여:
   - **대안 문구 제안**
   - **표준 문구 대비 차이점 요약**
   - **리스크 레벨 및 검토 포인트**를 제시한다.
3. 최종적으로 사용자가 검토/수정/확정할 수 있도록 **리포트 형태(마크다운/표/JSON 다운로드)** 로 제공한다.

> 목표는 “법무 업무를 대체”가 아니라, **초기 검토(1차 리딩) 시간을 줄이고 누락을 줄이는 보조 도구**다.

---

## 2) 주요 사용 대상
- **스타트업/중소기업**: 법무 인력이 부족한 조직의 대표/운영/재무 담당
- **프로덕트/세일즈/구매 담당자**: 계약서 초안 검토가 잦은 실무자
- **사내 법무/컴플라이언스 팀**: 반복되는 계약 검토에서 체크리스트/표준조항 매칭을 자동화하고 싶은 팀
- **외부 로펌/법무법인과 협업하는 담당자**: 이슈를 정리해서 전달해야 하는 사람

---

## 3) 유저 시나리오
### 시나리오 A: 계약서 업로드 → 리스크 요약 리포트 생성
1. 사용자가 Streamlit 앱에서 계약서 PDF/DOCX/TXT를 업로드
2. 시스템이 텍스트 추출 및 전처리(섹션/조항 단위 분리)
3. LangChain 체인 실행:
   - (1) 조항/섹션별 핵심 내용 요약
   - (2) 위험 조항 후보 탐지
   - (3) PydanticOutputParser로 스키마에 맞게 구조화 추출
4. 결과 화면에 “리스크 카드” 형태로 표시:
   - 리스크 레벨(High/Medium/Low)
   - 근거 문구(원문 인용 + 위치)
   - 왜 위험한지(해설)
   - 확인 질문(법무에 물어볼 질문)
5. 리포트 다운로드(마크다운/JSON)

### 시나리오 B: 표준 조항과 비교 → 대안 문구 추천
1. 사용자가 특정 리스크 항목(예: 책임 한도)을 클릭
2. 해당 조항 원문을 쿼리로 하여 VectorDB에서 표준 조항 검색(RAG)
3. 유사 표준 조항 Top-k + (선택) re-ranking 후 가장 적합한 표준 조항 선택
4. “표준 문구” + “현재 문구” + “추천 수정안”을 나란히 보여줌
5. 사용자는 추천 수정안을 복사하거나 리포트에 반영

### 시나리오 C: 외부 검색(검색 API)로 최신 규정/가이드라인 확인(옵션)
1. 사용자가 “개인정보/국외이전 조항이 최신 규정에 맞는지” 질문
2. 검색 API로 신뢰할 수 있는 공개 자료를 찾아 요약
3. 내부 표준 조항 + 외부 자료를 함께 근거로 답변 생성
4. 출처 링크/스니펫을 함께 제공(“출처 기반”)

### 시나리오 D: Tool Calling으로 내부 규칙/DB 연동(옵션)
1. 사용자가 “우리 회사 표준 책임한도 정책(예: 최대 1년치 이용료)과 충돌하나?” 질문
2. Tool Calling으로 내부 DB/설정(정책 테이블) 조회
3. 조회 결과를 근거로 충돌 여부 및 권장 방향 제시

---

## 4) 활용 기술
### 4.1 Streamlit
- 단일 페이지 혹은 멀티탭 UI
  - 업로드 탭: 파일 업로드, 계약 유형 선택, 옵션(엄격 모드/근거 표시/언어)
  - 분석 탭: 리스크 리스트, 조항별 비교 뷰, 근거 하이라이트
  - 라이브러리 탭: 표준 조항 관리(추가/수정/버전)
  - 내보내기 탭: 보고서 다운로드(마크다운/JSON)
- 캐싱 전략
  - 동일 문서 재분석 방지(해시 기반)
  - 벡터DB/임베딩 모델 로딩 캐시

### 4.2 LangChain
#### 4.2.1 PydanticOutputParser (구조화 추출)
- 계약서 분석 결과를 “사람이 읽는 텍스트”가 아니라 **검증 가능한 구조화 데이터**로 만든다.
- 예시 스키마(초안)
  - `ContractType`(NDA/Service/License/Etc)
  - `Parties`(갑/을, 관할, 국가 등 추출 가능하면)
  - `Findings[]`:
    - `clause_category` (예: Liability, Termination, Confidentiality, IP, Payment, GoverningLaw, Warranty, Indemnity, DataPrivacy...)
    - `risk_level` (High/Medium/Low)
    - `issue_summary` (한 줄 요약)
    - `evidence` (원문 발췌 + 조항 번호/페이지 추정)
    - `why_it_matters` (리스크 설명)
    - `questions_to_ask` (확인 질문 리스트)
    - `recommended_action` (수정/협상/확인)
- 추출 실패/불확실성 처리
  - `confidence` 필드(0~1)
  - 근거가 부족하면 “추정”으로 표시하고 사용자에게 재확인 유도

#### 4.2.2 Retrieval Augmented Generation (RAG)
**(1) File → vectorDB**
- 표준 조항 라이브러리(사내 템플릿, 권고 문구, 해설, 예시)를 문단/조항 단위로 청킹하여 임베딩 → VectorDB 저장
- 질의:
  - 계약서 원문 조항 텍스트
  - 또는 추출된 이슈 요약(`issue_summary`)
- 반환:
  - 유사 표준 조항 텍스트
  - 적용 조건/주의점(메타데이터로 보관)

**(2) 검색 API**
- 옵션 기능: 최신 공개 가이드/법령/기관 Q&A 등 “공개 자료”를 검색 API로 가져와 보강
- 주의: 법령 해석을 단정하지 않고, “참고 자료 요약” 형태로 제공 + 출처 표시

**(3) 심화: re-ranking, agentic RAG**
- re-ranking: Top-k 후보 표준 조항을 재정렬(쿼리-문서 적합도 향상)
- agentic RAG(선택):
  - “어떤 근거가 더 필요한지”를 에이전트가 판단해 추가 검색/추가 질의
  - 예: 준거법이 특정 국가인 경우 해당 국가 기준 체크리스트를 추가로 검색/적용

#### 4.2.3 Tool Calling
**(1) 내가 만든 자체 python 함수**
- 예: `extract_text(file)`, `split_into_clauses(text)`, `detect_language(text)`
- 예: `rule_based_flags(text)` (정규식/룰 기반으로 특정 위험 키워드 선탐지)
- 예: `render_report(findings, template="md")`

**(2) 외부 API 연동**
- 검색 API(공개 문서 검색)
- (선택) 문서 변환 API(DOCX→TXT 등) 또는 OCR

**(3) DB**
- 표준 조항 라이브러리 관리용 DB/VectorDB
- 사내 정책(예: 책임한도 정책, 개인정보 처리 기준, 준거법 선호값) 저장 DB
- 사용자 피드백(“이 추천은 유용/부정확”) 로그 저장

---

## 5) 출력물(초기 버전 범위)
- 업로드 계약서에 대한:
  - 리스크 항목 리스트(구조화 JSON)
  - 항목별 근거 문구(원문 인용)
  - 표준 조항 Top-3 추천 + 추천 수정안(텍스트)
  - 마크다운 리포트 다운로드

---

## 6) 비기능 요구사항(간단)
- **보안/프라이버시**
  - 업로드 문서는 민감 정보 가능성이 높으므로 저장 정책(세션 종료 시 삭제 등) 명확화
- **설명가능성**
  - 모든 리스크 판단은 “근거 문구”를 함께 제공(근거 없는 단정 금지)
- **확장성**
  - 표준 조항 라이브러리의 버전 관리(초기에는 메타데이터로만)

---

## 7) 리스크/주의사항(프로덕트 관점)
- 모델이 “법률 자문”처럼 단정하는 표현을 하지 않도록 가드 필요
- 계약서 포맷 다양성(PDF 스캔본/OCR 필요/표 형태) 대응 범위 명확히
- 표준 조항이 기업/산업마다 달라 “정답”이 없을 수 있으므로
  - 회사 정책/선호를 설정값/DB로 분리해 커스터마이즈 가능하게 설계